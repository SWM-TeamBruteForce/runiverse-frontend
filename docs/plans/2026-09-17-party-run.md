# 매칭 후 원격 동반 러닝 — 작업 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 매칭된 방으로 같은 시각에 각자 달리면서, 파티원의 진행률·격차·콤보를 화면에서 보고, 끝난 뒤 파티원과 기록을 비교한다.

**Architecture:** 새 인프라를 만들지 않는다. 솔로 러닝의 WebSocket 채널(`WsClient` → `WsRunningChannel`)과 `PartyBoard`·`partyProvider`가 이미 `RUNNING_PROGRESS_UPDATED`·`RUNNING_COMBO_UPDATED`를 받아 쌓고 있고, 대기실 → 카운트다운 → `openMatched(runningRoomId)` → 러닝 화면 전환도 되어 있다. **빈 곳을 메우고, 파티원 비교 화면을 정본(Figma S13 3/3)대로 다시 그리고, 두 기기로 끝까지 검증한다.**

**Tech Stack:** Flutter 3.44 (fvm) · Riverpod 3 · go_router 17 · `web_socket_channel` 3 · `geolocator` 14 · `sqflite`

**정본:** Figma `Ds2URinGrgXo43wC5OFQ8x` 노드 `4-24` (S11 출발 대기실 `43:46` · S11 카운트다운 `43:113` · S13 3/3 파티원 비교 `45:110`) · `와이어프레임_최종.md` S11 · S13 · S16 · S16.5 · 연동 가이드 5-1~6-2

---

## Global Constraints

- 모든 flutter/dart 명령은 fvm SDK로: `.\.fvm\flutter_sdk\bin\flutter.bat` / `dart.bat`
- 내부 import는 `package:runiverse/...` 절대경로 · 색·치수·문자열은 토큰만 · 아이콘은 Lucide만
- presentation → domain ← data · provider 의존은 `matching → session` 한 방향
- **GPS 좌표·경로를 파티원에게 노출하지 않는다 · 순위 숫자를 표시하지 않는다**
- 새 패키지 없음 (이 계획 범위에서는 필요하지 않다 — §8 참고)
- 커밋 `<이모지> <Type>: <설명>` · AI 공동 작성자 없음 · PR base `dev` · PR당 20파일/500줄 이하
- 각 태스크 끝에 analyze 0 · 테스트 통과 · 커밋 시점마다 빌드

---

## 1. 이미 있는 것 (다시 만들지 않는다)

| 영역 | 있는 것 | 위치 |
|---|---|---|
| WS 전송 | 연결·헤더 인증·401 갱신 1회·backoff 재연결·4001 중단·30초 HEALTH_CHECK | `lib/core/network/ws_client.dart` |
| 러닝 계약 | `RUNNING_START`(재연결 시 자동 재전송) · `RUNNING_LOCATION_UPDATE` 10초 배치(150점 상한·1009 대응) · `RUNNING_FINISH`/`FINISHED` ack 15초 · `ERROR` 코드 분기 | `session/data/ws_running_channel.dart`, `track_sender.dart` |
| 파티 도메인 | `RunProgress` · `ComboPeer`/`RunCombo`(전체 스냅샷 교체) · `PartyBoard`(진행률순 정렬·병합) | `session/domain/run_progress.dart`, `party_board.dart` |
| 파티 상태 | `partyProvider`(keepAlive, 채널 재바인딩, 명단 `setRoster`) | `session/presentation/party_provider.dart` |
| 화면 | S13 3페이지 스와이프 + 단순 막대 버전 `RunPartyView` | `run_session_page.dart`, `run_party_view.dart` |
| 전환 | `RUNNING_READY` → `launchAt` → `matchCountdown` → `_launch()`: 명단 인계 → `openMatched(room.runningRoomId)` → `start()` → `runSession` | `matching/presentation/match_countdown_page.dart` |
| 종료 | 중지 시트 · 80% 미만 `forced` 판정 · 사전 안내 | `run_session_page.dart`, `run_stop_sheet.dart` |
| 결과 | `/results` + `/split-results` 병합 조회, 1km 표·50m 그래프 | `record/data/http_run_record_repository.dart`, `room_result_dto.dart` |

## 2. 서버 계약 요약 (백엔드 소스 대조, 2026-09-17)

명세 정본: 노션 WEBSOCKET 명세서 › 메시지 › `RUNNING_PROGRESS_UPDATED`(No.7, BE 개발완료·FE 개발전) · `RUNNING_COMBO_UPDATED`. 아래는 소스와 대조해 확인한 내용이며 두 페이지의 필드와 규칙이 소스와 일치한다.

- 봉투 `{"event","data"}`. `RUNNING_STARTED`·`RUNNING_FINISHED`의 `data`는 **`{}`** — 명세의 스냅샷은 미구현. `RUNNING_PAUSE/RESUME` 없음(`paused`는 항상 false).
- `RUNNING_PROGRESS_UPDATED`: **참가자 한 명씩**, 배치마다(≈10초), 보낸 사람 제외. `userId · distanceMeters · targetDistanceMeters? · currentPaceSecondsPerKm? · paused`. 이름·사진·격차·비율 없음.
- `RUNNING_COMBO_UPDATED`: `peers[]{userId, gapMeters(+면 상대가 앞), comboCount, maxComboCount}`. **전체 상태**이고 빈 배열이 곧 콤보 해제. 콤보는 **쌍** 단위, 누적 거리 차 30m 이내, 19초 지나면 제외, 10초 tick.
- 끊김 통지 없음 — 상대 진행이 멈추고 19초 뒤 콤보에서 빠질 뿐이다.
- `/results`: `players[]{userId, nickname, profileImageUrl, status, isMe, isDeleted, totalDistance/Duration/Calories, averagePace, averageCadence, elevationGain}` — **파티원이 이미 들어 있다.** `/split-results`: 10m 구간마다 `players[]{userId, durationSeconds, averagePaceSecondsPerKm, …}` — **파티원 구간도 이미 온다.** 앱 DTO가 본인 것만 골라내고 버리는 중.
- 80% 미만 + 2인 이상 → 20분 쿨다운. `forced`는 의도 기록일 뿐 판정에 안 쓰인다. 결과 응답에 제재 여부는 없고 `/users/me/status`의 `cooldownUntil`만 남는다.

---

## 3. 단계

### 0단계 — 두 기기로 지금 코드를 끝까지 돌린다 (반나절)

만들기 전에 잰다. 매칭이 이제 되므로 동반 러닝 경로를 처음으로 실제 서버에서 돌릴 수 있다.

- [ ] 에뮬레이터 2대 GPS 재생 스크립트: 같은 경로를 5분 30초/km 안팎으로, 두 기기에 속도 차를 조금 두어 `adb emu geo fix`로 1초마다 밀어 넣는다 (scratchpad, 커밋 안 함)
- [ ] 시나리오: 신청(가까운 슬롯) → `RUNNING_READY` → 카운트다운 → 러닝 → 3페이지에서 상대 진행·콤보 확인 → 한쪽 조기 종료 → 다른 쪽 완주 → 결과
- [ ] 로그 태그로 확인: `RUNNING_STARTED` 도착 · `RUNNING_PROGRESS_UPDATED` 주기 · `RUNNING_COMBO_UPDATED` 진입/해제 · `RUNNING_FINISHED` · 결과 200
- [ ] 발견한 결함을 이 문서 §7에 적고 1단계 앞에 고칠 것을 고른다

### 1단계 — 파티원 비교 페이지를 정본대로 (Figma `45:110`)

**1-A. 순수 로직 (domain, 테스트 먼저)**

- [x] `PartyBoard.rows` 정렬 히스테리시스: 순위 교체는 격차가 10m 이상 확정될 때만 (FE노트 S13). 임계값은 상수 하나
- 파티원 페이스: **서버 `currentPaceSecondsPerKm`를 그대로 쓴다** (2026-09-17 결정). 표의 열 이름은 "페이스". 없으면 `--`
- [x] 참가자 색 자리: `PartyBoard.colorSlotOf` — 명단 순서(서버가 준 순서라 어느 기기에서나 같은 사람이 같은 자리). 화면이 자리 번호로 `RunHue`를 꺼낸다
- [x] 격차 부호·문구 규칙 한 곳(`PartyRow.gapMeters`): 상대가 앞이면 `+160m` success, 뒤면 `-120m` tertiary (Figma) — 서버 `combo.gapMeters` 우선, 없으면 거리 차

**1-B. 목표 거리 출처 고정**

- [x] `openMatched`에 `targetDistanceMeters`를 함께 넘긴다(`RoomInfo.targetDistanceMeters`). 첫 진행 통지(최대 10초)를 기다리지 않고 막대·눈금·80% 판정이 처음부터 선다. 복구 경로는 `UserStatusRunning.targetDistanceMeters`

**1-C. 화면 (presentation)**

- [ ] `RunPartyView` 재작성 — 레인 카드: 이름(내 것은 굵게) · 자기 색으로 칠한 막대 + 안쪽 흰 선 + 앞쪽 점 3개 + 머리 아바타(이니셜, 같은 색 테두리·글로우) · `AnimatedContainer`로 10초마다 이동 · 아래 0~목표 km 눈금(막대와 같은 좌표계)
- [ ] 표: 러너 / 거리(km, tabular) / 평균 페이스. 내 행 `primary-muted` 배경, 이름 옆 격차. 콤보 중인 상대는 이름 옆 작은 콤보 배지(`comboCount`)
- [ ] 머리·꼬리 문구: "함께 달리는 중 · 각자의 색으로 칠해요" / "공유되는 것은 진행률과 격차뿐이에요" — `AppStrings`
- [ ] 빈 상태(명단 없음·아직 통지 없음)와 목표 거리 없음 상태를 정의한다. 이름을 모르는 참가자는 `함께 달리는 사람`
- [ ] 콤보 피드백: 콤보 진입·해제 순간 `HapticFeedback` 한 번(패키지 없음). 소리는 범위 밖
- [ ] 위젯 테스트: 4명·1명·빈 명단, 목표 없음, 순위 교체 애니메이션 없이 순서 유지

### 2단계 — 출발 대기실(S11)을 GPS를 잡는 화면으로

지금 `matchCountdown`은 카운트다운만 그리고 **GPS fix를 기다리지 않는다** — 솔로는 기다린다. 매칭 러닝의 첫 좌표가 부정확해질 수 있다.

- [ ] S11 레이아웃: `시작까지 0:12` · 세션 바(시작 시간 │ 목표 거리) · 표시 전용 지도 + `● GPS 신호 양호/찾는 중` · 파티원 아바타(색 적용) · 하단은 **버튼 없이** 안내("출발 시각에 자동으로 시작해요"). 준비 완료 버튼·체크는 서버에 참가자 준비 상태가 없어 뺀다(기존 결정 유지)
- [ ] 30초 전 진입 시 위치 스트림을 미리 열어 fix를 확보한다(솔로 `run_prepare_page`의 로직 재사용, 화면은 공유하지 않는다)
- [ ] 3초 전 160px 숫자 + 햅틱은 지금 것 유지, 뒤로가기 차단 유지

### 3단계 — 결과 화면의 파티원 비교 (S16 · S16.5, 가이드 6-1·6-2)

데이터는 이미 응답에 있다. DTO만 넓히면 된다.

- [ ] `RoomResultDto`: `players[]` 전원을 `RunPlayerResult`로, `splits[].players[]`를 `Map<userId, List<RawSplit>>`로 읽는다. 본인 판별은 `isMe`
- [ ] `RunDetail`에 `players`·`splitsByPlayer` 추가. 기존 본인 필드는 그대로
- [ ] S16 `파티원 비교` 카드: 색 점 · 닉네임 · 완주 시간 · 평균 페이스. 기록 없는 참가자(metrics null)는 `기록 없음`. **팔로우 버튼은 뺀다** — API 미확인
- [ ] S16.5 러너 칩: 나 고정 + 최대 1명 겹쳐보기(정본은 2명 — 첫 버전은 1명). 1km 표에 격차 열(success/error) · 50m 페이스 그래프에 두 번째 선
- [ ] 테스트: 파서(2인·탈퇴자·기록 없는 참가자), 격차 계산

### 4단계 — 동반 러닝의 끊김·복구

- ~~명단 보존(닉네임·사진 로컬 저장)~~ **보류 (2026-09-17 결정).** 앱 재시작 후 복구하면 파티원이 `함께 달리는 사람`으로 뜬다. `RUNNING_STARTED` 스냅샷이 구현되면 그쪽으로 푼다
- [ ] `NOT_ROOM_PLAYER` 수신 시 러닝을 접고 상태 재조회 → 홈 (명시적으로 나간 사람이 받는 코드)
- [ ] `RUNNING_TRACK_UNAVAILABLE` 반복을 배너 하나로 접는다(가이드 요구)
- [ ] 상대 정지 표시: 19초 넘게 통지가 없으면 레인을 흐리게(연결 끊김 추정). 서버 통지가 없으므로 앱이 시간으로 판단한다 — 문구 없이 흐리게만

---

## 4. 결정 (2026-09-17 확정)

| # | 질문 | 결정 |
|---|---|---|
| 1 | 레인 색 | `RunHue` 10색을 명단 순서로 배정 — 결정론적, 서버 변경 없음 |
| 2 | 파티원 비교 표의 페이스 | 서버 `currentPaceSecondsPerKm` 그대로, 열 이름 "페이스" |
| 3 | S11 준비 완료 버튼 | 뺀다(서버에 준비 상태 없음), GPS fix 표시만 |
| 4 | 3단계 범위 | 이번에 포함 |
| 5 | 콤보 피드백 | 햅틱만 |
| 6 | 파티원 이름·사진 로컬 보존 | 보류 |

**순서 조정:** 매칭 슬롯은 18:00~22:00 KST뿐이라 0단계(두 기기 실주행)는 당일 18:00 이후에만 가능하다. 낮에는 1단계(도메인 → 화면)를 먼저 하고, 18:00 슬롯으로 0단계를 돌린 뒤 §7을 채운다.

## 5. PR 분할 (순차 머지)

1. `fix/party-run-baseline` — 0단계에서 나온 결함 (있으면)
2. `feat/party-board-rules` — 1-A + 1-B (domain·provider, 테스트)
3. `feat/party-compare-view` — 1-C (화면·위젯 테스트, 스크린샷)
4. `feat/match-start-room` — 2단계
5. `feat/run-result-party` — 3단계 (DTO·domain) / `feat/run-result-party-view` (화면) 둘로 나눌 수 있다
6. `fix/party-run-recovery` — 4단계

## 6. 테스트 전략

- 순수 로직: 히스테리시스 정렬 · 평균 페이스 · 색 배정 결정론(같은 명단 → 같은 색) · 격차 부호 · 다중 참가자 파서
- 상태 전이: `openMatched(target)` → 첫 통지 전 막대 표시 · `NOT_ROOM_PLAYER` → 종료
- 위젯: `RunPartyView` 4상태 · S16 카드 2상태
- 기기: 0단계 스크립트를 각 PR 끝에 다시 돌린다(두 기기, Wi-Fi 끈 상태, keepalive 터널)

## 7. 0단계에서 발견한 결함

코드를 읽다 먼저 찾은 것 (0단계에서 재현 확인):

- [x] **명단에 내가 들어 있었다.** `_launch()`가 `room.players` 전원을 넘기는데 서버는 내 진행을 보내지 않아, 내 이름 줄이 0m로 "나" 레인 옆에 또 뜰 상황이었다. → 1-A에서 `myUserId`로 가려냈다(`c8375cb`)
- [ ] **러닝 복구가 다시 붙지 않는 것으로 보인다.** 앱 재시작 → `RUNNING` → `runSession`을 열지만, 그 화면은 `WakelockPlus.enable()`만 하고 연결·세션을 시작하는 코드가 없다. 0단계에서 실제로 확인하고 4단계에 넣는다

## 8. 범위 밖 (이유와 함께)

| 뺀 것 | 이유 |
|---|---|
| S14 컬러 리빌 · 블렌드 색 · 목표 도달 자동 완주 | 색 생성 규칙 미정(디자인 시스템 §5 "제품/서버가 확정") |
| 음성 코치 · "3,2,1,출발" 음성 · 음소거 토글 | 오디오 패키지 필요 → 별도 승인. 상단 바의 mic 아이콘은 자리만 잡고 비활성 |
| 일시정지 공유 | `RUNNING_PAUSE/RESUME` 서버 미구현 |
| S10 로비 리액션 | 서버 이벤트 없음 |
| S16 팔로우 버튼 | API 미확인 |
| 재연결 스냅샷(`RUNNING_STARTED` data) | 서버 미구현 — 4단계의 로컬 명단 보존으로 대신 |

## 9. 백엔드에 물어볼 것

- `RUNNING_STARTED` 스냅샷(참가자 현재 거리) 구현 예정인가 — 되면 4단계 로컬 보존이 보조로 내려간다
- 참가자 시그니처 컬러를 `players[]`에 실을 계획이 있는가 — 있으면 1-A 색 배정을 그 값으로 바꾼다
- 솔로 방의 강제 종료 스케줄 부재(feature-spec 312) — 이 계획과 무관하지만 러닝 복구 409에 영향
