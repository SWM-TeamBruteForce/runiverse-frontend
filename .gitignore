# OS
.DS_Store
Thumbs.db

# Miscellaneous
*.class
*.log
*.pyc
*.swp
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# FVM — .fvmrc(버전 핀)만 커밋하고 .fvm/ 안은 전부 무시한다.
# .fvm/flutter_sdk 와 .fvm/versions/ 는 Flutter SDK 실체라 커밋하면 저장소가 통째로 부풀고,
# 나머지(fvm_config.json 등)는 로컬 캐시라 팀원 간에 의미가 없다.
.fvm/

# 서명 키 · 시크릿 — 절대 커밋 금지
/android/key.properties
/android/app/*.jks
*.env

# 빌드 설정 — 실제 키가 들어간다. 예시 파일만 공유한다.
#
# `--dart-define-from-file=config/dev.json`으로 주입한다. 명령줄에 키를 적으면
# 셸 히스토리·프로세스 목록에 평문으로 남는데, 파일로 넘기면 그 경로가 없다.
config/*.json
!config/*.example.json

# Claude Code 개인 설정 — 팀 저장소에 섞지 않는다.
.claude/
